---
name: mastergo-design-to-code
description: 使用 MasterGo Magic MCP 读取设计稿 DSL、SVG、文本与组件信息，并在任意现有代码库中完成界面实现、重构、视觉校验或问题修复，且严格以成功获取完整 DSL 为实施前提。适用于用户提供 MasterGo 链接、文件 ID、图层 ID、截图或要求严格还原设计稿的任务。
---

# MasterGo 设计转代码

## 目标

将 MasterGo 设计数据转化为符合当前代码库约定、可维护且能适应真实业务数据的界面。不要把本 Skill 当作固定页面模板；每次先理解仓库、设计范围和用户最新要求，再选择实现方式。

## 执行流程

1. 读取当前仓库的 Agent 规则、开发规范、依赖、公共组件和相邻实现，确认技术栈、目录职责、样式方案和验证命令。
2. 检查 MasterGo MCP 是否已安装或已配置。存在可用服务时直接复用；仅在缺失时按官方仓库说明安装。认证信息从运行环境或 MCP 配置读取，禁止写入 Skill、源码、日志和交付文件。
3. 在修改代码前通过 DSL 前置门禁：使用 `getDesignSections` 获取设计范围概览，分批读取全部 section，并确认目标范围的数据完整且可解析；随后获取 SVG 和长文本。详细流程见 [references/mastergo-mcp.md](references/mastergo-mcp.md)。
4. 如果没有可用认证凭据，或因认证、权限、链接、服务、网络等问题无法取得完整 DSL，先完成必要诊断和有限重试。仍然失败时，明确告知用户阻断原因、失败环节和需要补充的内容，然后中断任务；不得开始或继续界面开发，也不得仅凭截图、预览图或经验推测设计细节。
5. 从 DSL 提取视觉契约：画布、层级、位置、尺寸、间距、圆角、边框、字体、行高、颜色、渐变、图片、图标、状态和交互。
6. 区分固定格式控件与动态业务内容。固定控件可以使用稳定尺寸；标题、描述、列表和可变数据优先使用正常文档流与内容驱动布局。
7. 按当前仓库模式拆分组件并复用已有能力。不要引入与仓库不一致的框架、路由、状态方案、样式体系或新抽象。
8. 在 DSL 已成功获取的前提下，使用设计截图和真实数据进行视觉与交互校验，修复溢出、抖动、遮挡、滚动回弹、图片变形和不同平台字体偏移。
9. 运行仓库规定的类型检查、静态检查、测试或构建，并检查改动范围、编码和残留临时资源。

## 核心原则

- 成功获取完整、可解析的 DSL 是开始开发的硬性前置条件。截图只能在此之后用于校验整体观感和 DSL 难以表达的细节，不能作为 DSL 缺失时的替代输入。
- 用户在后续消息中的修正覆盖早期要求；实现前整理最终有效要求，避免保留已废弃的资源或交互。
- 优先复用代码库的图片、弹框、展开收起、轮播、图标和安全区组件；使用其公开参数和插槽，不复制内部实现。
- 以组件化思维合理拆分页面与组件：按职责边界组织文件，禁止将全部实现堆放在单个文件中。
- 动态标题或浮层从隐藏到显示时，使用不占文档流的过渡方案，避免页面跳动。
- 业务卡片和文本区域不要因设计稿示例数据而盲目固定高度；需要截断时明确行数或裁剪规则。
- 多状态、多分组或连续媒体交互应由单一状态源驱动，切换当前状态时避免重复操作，并保留用户合理的浏览位置。
- 图片按设计比例使用 cover/contain；资源不可用时遵循任务要求使用占位，不要擅自引入无来源资源。
- 图标必须保持原始宽高比。若有效图形只占资源的一部分，使用裁切容器处理，不要直接压缩资源。
- 渐变、复杂任意值和低复用样式可使用行内样式或局部样式；高复用布局优先遵循仓库现有样式体系。
- 针对移动端验证滚动边界、1px 边框、安全区、触摸滑动、回弹效果和平台字体对齐。

## 完成标准

- 开始实现前已通过 DSL 前置门禁；若门禁失败，已说明原因并停止开发，没有基于截图或经验生成猜测性实现。
- 已读取完整设计范围，没有漏取 section、SVG、长文本或重要状态。
- 实现符合当前仓库架构，没有硬编码本 Skill 之外的目录或框架假设。
- 长文本、空数据、缺图、多列表项和不同状态不会破坏布局。
- 关键交互在重复点击、边界切换、展开收起和滚动时行为稳定。
- 设计稿主要尺寸、字体、颜色、圆角、间距和层级均有 DSL 或截图依据。
- 代码检查通过，没有无关重构、临时文件、凭据或本地资源残留。

## 参考资料

- [MasterGo MCP 工作流](references/mastergo-mcp.md)
- [通用设计还原方法](references/design-reproduction.md)
- [框架无关的实现与验证规范](references/implementation-quality.md)
